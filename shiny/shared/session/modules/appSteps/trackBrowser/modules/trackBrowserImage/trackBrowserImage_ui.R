# trackBrowser UI module for assembling the composite browser output image
# there may be one or multiple images rendered by a single browser instance
trackBrowserImageUI <- function(id, ...) {
    ns <- NS(id) 
    tags$div(

    )
}
