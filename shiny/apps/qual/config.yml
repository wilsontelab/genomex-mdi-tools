---
name: qualPlots
description: "View the distribution plots of quality scores generated by genomex-mdi-tools/qual"
# version: v0.0.0 # OPTIONAL
# suiteVersions:
#     suiteName: v0.0.0
uploadTypes:
    genomex-qual: 
        contentFileTypes:
            qualPlots:  
                required: true
appSteps:
    upload: # nearly always the 1st step of every app
        module: sourceFileUpload
    view:
        module: viewPlots
